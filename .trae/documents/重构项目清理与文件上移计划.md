# 重构项目清理与文件上移计划

## 一、Summary 概要

重构目标已完成，将 `free/` 目录下的 Nuxt 新项目内容上移到根目录，并彻底删除原 `magic-resume/` 旧项目。最终根目录 `/Users/songang/LinkProjects/ziyoujianli/` 即为新项目根。

## 二、Current State Analysis 当前状态分析

### 2.1 根目录结构

```
/Users/songang/LinkProjects/ziyoujianli/
├── .idea/                  # IDE 配置（保留）
├── .trae/                  # Trae 文档与 specs（保留）
│   └── documents/
│       ├── magic-resume-架构分析与新项目选型建议.md
│       └── magic-resume到Vue3-TDesign迁移方案.md
├── free/                   # 新 Nuxt 项目（无 .git）
│   ├── .github/workflows/  # CI 配置
│   ├── .gitignore
│   ├── .nuxt/              # ⛔ 生成物 - 排除
│   ├── app/                # 源码
│   ├── coverage/           # ⛔ 生成物 - 排除
│   ├── i18n/
│   ├── node_modules/       # ⛔ 生成物 - 排除
│   ├── nuxt.config.ts
│   ├── package.json
│   ├── playwright.config.ts
│   ├── playwright-report/  # ⛔ 生成物 - 排除
│   ├── pnpm-lock.yaml      # 保留
│   ├── public/
│   ├── scripts/
│   ├── server/
│   ├── shared/
│   ├── test-results/       # ⛔ 生成物 - 排除
│   ├── tests/
│   ├── tsconfig.json
│   ├── vitest.config.ts
│   └── README.md
└── magic-resume/           # 旧项目（含独立 .git，将被彻底删除）
    ├── .git/
    ├── .github/
    ├── src/...
    └── ...
```

### 2.2 关键事实

- 根目录非 git 仓库（无 .git）
- `free/` 与 `magic-resume/` 互不依赖（free 为全新重写）
- `magic-resume/` 含独立 `.git` 历史，删除后无法恢复（用户已确认彻底删除）
- 终端 3 正在运行 `pnpm dev`（命令 ID `d2631245-8b58-4667-96ea-d7bbb6f72c8e`），需先停止
- `free/.gitignore` 已配置正确（忽略 .nuxt、node_modules、coverage、test-results、playwright-report、.env 等）

## 三、Proposed Changes 实施步骤

### Step 1: 停止运行中的 dev server

- 调用 `StopCommand` 停止终端 3 的命令 ID `d2631245-8b58-4667-96ea-d7bbb6f72c8e`
- 原因：文件移动过程中 dev server 持有 `.nuxt` 等文件句柄，可能导致移动失败

### Step 2: 删除 magic-resume/ 目录

- 执行 `rm -rf /Users/songang/LinkProjects/ziyoujianli/magic-resume`
- 彻底删除旧项目（含 .git 历史），用户已确认不可恢复

### Step 3: 移动 free/ 内容到根目录

采用"逐项移动、排除生成物"策略，避免 node_modules 等大量文件移动耗时：

1. 使用 shell 批量将以下内容从 `free/` 移到根目录：
   - `app/`、`server/`、`shared/`、`public/`、`scripts/`、`tests/`、`i18n/`
   - `.github/`
   - `package.json`、`pnpm-lock.yaml`、`nuxt.config.ts`、`tsconfig.json`
   - `playwright.config.ts`、`vitest.config.ts`
   - `.gitignore`、`README.md`

2. 移动命令使用 `mv` 配合通配符，显式列出要移动的项（不使用 `mv free/* .`，因为后者会包含 node_modules 等大目录）。

3. 排除的生成物：`.nuxt/`、`node_modules/`、`coverage/`、`test-results/`、`playwright-report/`

### Step 4: 删除空的 free/ 目录及残留生成物

- `rm -rf /Users/songang/LinkProjects/ziyoujianli/free`
- 此时 `free/` 仅剩排除的生成物，整体删除即可

### Step 5: 重新安装依赖

- 在根目录执行 `pnpm install`
- 恢复 `node_modules`，并触发 `postinstall: nuxt prepare` 重新生成 `.nuxt`

### Step 6: 验证

- `ls -la` 检查根目录结构，确认 magic-resume 与 free 均已不存在，且 free 的源码与配置已就位
- 执行 `pnpm dev`（使用现有空闲终端 5 或 6）启动 dev server，确认无错误
- 检查 `pnpm dev` 输出，确认 Nuxt 正常启动（监听 3000 端口）

## 四、Assumptions & Decisions 假设与决策

| 项 | 决策 |
|---|---|
| magic-resume 删除方式 | 直接彻底删除（含 .git），不可恢复 — 用户已确认 |
| 根目录 git 初始化 | 暂不初始化，由用户后续自行处理 |
| 生成物处理 | 排除 node_modules / coverage / test-results / playwright-report / .nuxt，移动后重新 pnpm install |
| .trae 目录 | 保留原位（含历史规划文档作为参考） |
| .idea 目录 | 保留原位（IDE 配置） |
| .trae/specs/build-ziyou-resume-vue | 保留原位（实施 spec 文档） |

## 五、Verification 验证步骤

1. 根目录 `ls` 输出应仅包含：`.github` `.idea` `.trae` `app` `i18n` `node_modules`(重装后) `public` `scripts` `server` `shared` `tests` + 顶层配置文件
2. `magic-resume/` 与 `free/` 均不存在
3. `pnpm dev` 成功启动，无 import 错误
4. 可选：访问 `http://localhost:3000` 确认页面可访问

## 六、Risk & Rollback 风险与回滚

- **风险**：`magic-resume/` 删除不可逆。`free/` 文件移动过程中若中断可能造成文件散落。
- **缓解**：先停止 dev server；按"删除 magic-resume → 移动 free 内容 → 删除空 free"顺序执行，每步可单独验证。
- **回滚**：magic-resume 一旦删除无法回滚；free 的源码移动失败可重新从备份恢复（用户需自备外部备份，本次操作不涉及）。
